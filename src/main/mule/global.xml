<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:tls="http://www.mulesoft.org/schema/mule/tls"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
        http://www.mulesoft.org/schema/mule/tls http://www.mulesoft.org/schema/mule/tls/current/mule-tls.xsd">

    <!-- Configuration globale des propriétés -->
    <configuration-properties file="config.properties"/>

    <!-- Configuration HTTP Listener -->
    <http:listener-config name="HTTP_Listener_config" doc:name="HTTP Listener config" doc:id="b95be154-a770-461b-8cbc-42a31e9a8e2e">
        <http:listener-connection host="0.0.0.0" port="${http.port}">
        </http:listener-connection>
    </http:listener-config>

    <!-- Configuration HTTP Request pour Anypoint APIs -->
    <http:request-config name="Anypoint_HTTP_Request_config" doc:name="HTTP Request config">
        <http:request-connection host="eu1.anypoint.mulesoft.com" protocol="HTTPS" port="443" />
    </http:request-config>

    <!-- Object Store pour stocker le token -->
    <os:object-store name="Token_Store" doc:name="Token Object Store" persistent="false" maxEntries="10" entryTtl="3500" entryTtlUnit="SECONDS"/>

    <!-- Object Store pour l'historique des alertes -->
    <os:object-store name="Alert_History_Store" 
                      doc:name="Alert History Store" 
                      persistent="true" 
                      maxEntries="100" 
                      entryTtl="30" 
                      entryTtlUnit="DAYS"/>

    <!-- Object Store pour Slack Token -->
    <os:object-store name="Slack_Token_store" doc:name="Slack Token Store" doc:id="dcbf5f33-e4dd-4f7e-80c5-bedf7db9e6b1" maxEntries="5" entryTtl="30" entryTtlUnit="DAYS"/>

    <!-- Variables globales pour les limites -->
    <global-property name="limit.flows.warning" value="250" />
    <global-property name="limit.flows.critical" value="300" />
    <global-property name="limit.governed.warning" value="9" />
    <global-property name="limit.governed.critical" value="12" />
    <global-property name="limit.managed.warning" value="9" />
    <global-property name="limit.managed.critical" value="12" />
    
    <!-- Configuration de l'organisation -->
    <global-property name="org.id" value="f22cd53d-c1ea-482e-a6e6-2d367ba7e48e" />
    <global-property name="org.name" value="BNDE" />
    
    <!-- Configuration Slack -->
    <global-property name="slack.channel" value="#bnde-alerts" />

</mule>

